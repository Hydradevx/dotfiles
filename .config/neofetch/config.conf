# Logo
ascii_distro="arch_small"
# (you can list logos with: neofetch --list_ascii)

# Padding
gap=2
padding=2

# Separator
separator="  "

# Info function (controls order + labels)
print_info() {
    info title
    info underline

    info "OS     " distro
    info "DE     " de
    info "WM     " wm
    info "Packages" packages
    info "Shell  " shell
    info "Kernel " kernel
    info "Uptime " uptime
    info "OS Age " os_age
    info "Memory " memory

    prin "" ""
    info "Colors" blocks
}

# Function to calculate OS age
os_age() {
    local birth_install=$(stat -c %W /)
    local current=$(date +%s)
    local days_difference=$(( (current - birth_install) / 86400 ))
    echo "$days_difference days"
}

# Colors (similar to fastfetch’s circles)
colors=(4 6 2 3 5 1)   # blue, cyan, green, yellow, magenta, red
color_blocks="on"
block_range=(0 15)
block_width=3
block_height=1